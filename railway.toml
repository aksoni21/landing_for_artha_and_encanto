[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 900
restartPolicyType = "on_failure"

[[services]]
name = "web"
port = "3000"

[deploy.environment]
NODE_ENV = "production" 